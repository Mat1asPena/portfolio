<!-- quien soy + rol + CTA -->

---

const name = "Matias Agustin Peña";
const role = "Full-Stack Developer focalizado en backend";
const description = `
Desarrollador fullstack, con experiencia en Angular, JavaScript, TypeScript, HTML5, CSS3, Python, Node/Nest, Bases de datos SQL y noSQL.
Me enfoco en crear aplicaciones claras, funcionales
y bien estructuradas.
`;

---

<section class="hero">
    <div class="hero-content">
        <p class="hero-kicker">Hola, soy</p>
        <h2>{name}</h2>
        <h3>{role}</h3>

        <p class="hero-description">
        {description}
        </p>

        <div class="hero-actions">
        <a href="#projects" class="btn primary">Ver proyectos</a>
        <a href="/cv.pdf" class="btn secondary" target="_blank">Descargar CV</a>
        </div>
    </div>
</section>

<style>
.hero {
    min-height: 90vh;
    display: flex;
    align-items: center;
    padding: 4rem 1.5rem;
}

.hero-content {
    max-width: 720px;
}

.hero-kicker {
    color: #7dd3fc;
    font-weight: 600;
    margin-bottom: 0.5rem;
}

h1 {
    font-size: clamp(2.5rem, 5vw, 3.5rem);
    line-height: 1.1;
    margin-bottom: 0.5rem;
    }

h2 {
    font-size: 1.5rem;
    font-weight: 500;
    color: #94a3b8;
    margin-bottom: 1.5rem;
}

.hero-description {
    font-size: 1.05rem;
    line-height: 1.7;
    color: #cbd5f5;
    margin-bottom: 2rem;
}

.hero-actions {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
}

.btn {
    padding: 0.75rem 1.5rem;
    border-radius: 8px;
    font-weight: 500;
    text-decoration: none;
    transition: all 0.2s ease;
}

.btn.primary {
    background: #38bdf8;
    color: #020617;
}

.btn.primary:hover {
    background: #0ea5e9;  
}

.btn.secondary {
    border: 1px solid #38bdf8;
    color: #38bdf8;
}

.btn.secondary:hover {
    background: rgba(56, 189, 248, 0.1);
}
</style>